<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HealthMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/medical_blue</item>
        <item name="android:windowBackground">@color/medical_background</item>
        <item name="android:statusBarColor">@color/medical_blue</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.HealthMate.Splash" parent="Theme.HealthMate">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>